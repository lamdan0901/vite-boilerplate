/** @type {import("prettier").Config} */
const config = {
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  trailingComma: "none",
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
